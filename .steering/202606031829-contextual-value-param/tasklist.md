# タスクリスト

## 🚨 タスク完了の原則

**このファイルに計画した実行可能タスクは、完了または技術的理由付きの blocked/skipped として明示すること**

### 必須ルール
- **完了したタスクは `[x]` にすること**
- **作業終了時に、理由のない未完了タスク（`[ ]`）を残さないこと**
- 継続できないタスクは、技術的理由を明記して blocked/skipped として扱うこと
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止

### 実装可能なタスクのみを計画
- 計画段階で「実装可能なタスク」のみをリストアップ
- 「将来やるかもしれないタスク」は含めない
- 「検討中のタスク」は含めない

### blocked/skipped が許可されるケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった
- 技術的な理由により実装不可と判断した

blocked/skipped 時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
- [x] ~~タスク名~~（blocked: 依存Modの1.21.1対応版が未確認のため。次に必要な判断: 対応版確認後に別タスク化）
```

### タスクが大きすぎる場合
- タスクを小さなサブタスクに分割
- 分割したサブタスクをこのファイルに追加
- サブタスクを1つずつ完了させる

### フェーズ追加ルール
- 追加フェーズは既存の最終フェーズ番号 +1 で末尾に連番で追記する（途中挿入しない）
- ステアリング文書の作成自体はタスクに含めない

---

## 対象

- 対象バージョン: 1.21.1
- 対象ディレクトリ: `C:\Users\htv432\IdeaProjects\psitweaks\1.21.1-worktree`
- 検証コマンド:
    - [x] `.\gradlew runData`（対象ディレクトリで実行）
    - [x] `.\gradlew compileJava`（対象ディレクトリで実行）

## フェーズ1: Param 実装

- [x] `SpellParamContextualValue` を追加する（Codex作業）
- [x] `演算子: タグリスト` の入力を新 Param に差し替える（Codex作業）
- [x] Vector 単体入力を対象外にし、説明文と処理を一致させる（Codex作業）

## フェーズ2: datagen と検証

- [x] `psitweaks.datatype.contextual_value` を provider に追加する（Codex作業）
- [x] `.\gradlew runData` を実行する（Codex作業）
- [x] `.\gradlew compileJava` を実行する（Codex作業）

## フェーズ3: 品質チェックと修正

- [x] 旧 `ParamAny` 利用や Vector 受け入れ説明の残存を確認する（Codex作業）
- [x] 実装後の振り返りを記録する（Codex作業）

---

## 実装後の振り返り

### 作業者
本タスクは Codex が実施

### 実装完了日
2026-06-03

### 計画と実績の差分

**計画と異なった点**:
- `SpellParamContextualValue` は `ParamSpecific` ではなく `SpellParam<Object>` として実装した。Entity と `ContextualValue` を同じ required type で扱うため、`canAccept` を明示的に絞る方が Psi の接続判定に合うため。

**新たに必要になったタスク**:
- なし

**技術的理由で blocked/skipped にしたタスク**（該当する場合のみ）:
- なし

### 学んだこと

**技術的な学び**:
- 複数の継承関係にまたがる入力カテゴリは、`Object` required type と `canAccept` の組み合わせで UI 上のデータタイプとして表現できる。

**プロセス上の改善点**:
- 前回の `ParamAny` 暫定実装を専用 Param に置き換えることで、表示と実処理の受け入れ範囲を揃えられた。

### 次回への改善提案
- `ContextualValue` 実装型を増やす場合は、`getTagIds` などの共通口も同時に検討する。
